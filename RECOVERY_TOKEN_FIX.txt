RECOVERY TOKEN FIX V2

Token persistence now uses a resilient cloud writer.
The writer accepts every successful 2xx response, logs storage failures, retries through a recovery mirror stored inside the existing users cloud object, and reads that mirror when the dedicated token object is unavailable.
Bulk creation is still one atomic logical write: no token is reported successful until persistence succeeds.
